<Window x:Class="Diplom_zxc.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Diplom_zxc.ViewModels"
        Title="Diplom_zxc - Вход" 
        Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.DataContext>
        <viewModels:LoginViewModel/>
    </Window.DataContext>

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Border Background="{DynamicResource MaterialDesignCardBackground}" 
                CornerRadius="10"
                Padding="30"
                Margin="20"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Diplom_zxc" FontSize="28" FontWeight="Bold" 
                          HorizontalAlignment="Center" Margin="0,0,0,10"
                          Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                <TextBlock Text="Вход в систему" FontSize="18" 
                          HorizontalAlignment="Center" Margin="0,0,0,30"
                          Foreground="{DynamicResource MaterialDesignBody}"/>

                <TextBlock Text="Имя пользователя" Margin="0,0,0,5"
                          Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Height="35" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль" Margin="0,0,0,5"
                          Foreground="{DynamicResource MaterialDesignBody}"/>
                <PasswordBox x:Name="PasswordBox"
                            Height="35" FontSize="14" Margin="0,0,0,5"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                          Foreground="Red" 
                          Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                          Margin="0,5" TextWrapping="Wrap"/>

                <Button Content="Войти" 
                       Command="{Binding LoginCommand}"
                       Height="40" FontSize="14"
                       IsEnabled="{Binding IsLoggingIn, Converter={x:Static local:InverseBoolConverter.Instance}}"
                       Background="{DynamicResource PrimaryHueMidBrush}"
                       Foreground="White"
                       BorderThickness="0"
                       Margin="0,15,0,10"/>

                <TextBlock HorizontalAlignment="Center" Margin="0,10">
                    <Hyperlink Command="{Binding ForgotPasswordCommand}" 
                              Foreground="{DynamicResource PrimaryHueMidBrush}">
                        Забыли пароль?
                    </Hyperlink>
                </TextBlock>

                <Button Content="Создать аккаунт" 
                       Command="{Binding GoToRegisterCommand}"
                       Height="35" FontSize="12"
                       Background="Transparent"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       BorderBrush="{DynamicResource MaterialDesignDivider}"
                       BorderThickness="1"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>